# SilverFox schema reference — apply with: python backend/manage.py migrate
# Or: psql $DATABASE_URL -f db/schema.sql (after replacing with full dump if needed)
#
# Canonical migrations live in backend/*/migrations/
